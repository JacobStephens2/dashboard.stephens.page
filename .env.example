# Dashboard own config
ADMIN_PASSWORD_HASH=  # bcrypt hash, generate with: python -c "from passlib.hash import bcrypt; print(bcrypt.hash('your-password'))"
SESSION_SECRET=  # random 64-char hex, generate with: python -c "import secrets; print(secrets.token_hex(32))"

# Per-app database paths / credentials
CREIGHTON_DB=/var/www/creighton.stephens.page/server/data/creighton.db
MACROS_DB=/var/www/macros.stephens.page/server/data/macros.db
DAILYDOZEN_DB=/var/www/dailydozen.stephens.page/data/daily_dozen.db

ARTIFACT_DB_HOST=localhost
ARTIFACT_DB_USER=stewardg_db_user
ARTIFACT_DB_PASS=
ARTIFACT_DB_NAME=stewardg_artifacts

EXODUS_DB_HOST=localhost
EXODUS_DB_USER=exodus40lite
EXODUS_DB_PASS=
EXODUS_DB_NAME=exodus40lite

GAMEPLAN_DB_HOST=localhost
GAMEPLAN_DB_USER=gameplanning_user
GAMEPLAN_DB_PASS=
GAMEPLAN_DB_NAME=gameplanning_database

EVENT_DB_HOST=localhost
EVENT_DB_USER=eventmanageruser
EVENT_DB_PASS=
EVENT_DB_NAME=event_manager_database

SKYLAR_DB_HOST=127.0.0.1
SKYLAR_DB_USER=skylar_user
SKYLAR_DB_PASS=
SKYLAR_DB_NAME=skylar_members_db
